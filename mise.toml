[tools]
ruby = "4.0.2"
hk = "1.45.0"
pkl = "0.31.1"

[tasks.test]
description = "Runs the full test suite."
run = "bundle exec rspec"

[tasks.fix]
description = "Runs lint checks and applies automatic fixes."
run = "hk fix"
